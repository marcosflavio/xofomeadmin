<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.teste.xofome.xofomeadmin">

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service
            android:name=".service.AdicionaProdutoService"
            android:exported="false">
        </service>

        <service
            android:name=".service.AtualizaPedidoService"
            android:exported="false">
        </service>

        <service
            android:name=".service.ListaPedidosService"
            android:exported="false">
        </service>

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AddProdutoActivity" />
        <activity android:name=".activities.DescricaoPedidoAcitivity"></activity>
    </application>

</manifest>